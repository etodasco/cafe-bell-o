<div class="d-flex justify-content-center align-items-center" style="height: 100vh;">
  <div class="card text-center" style="width: 18rem;">
    <img src="path_to_image_or_placeholder.jpg" class="card-img-top" alt="<%= @menu_item.name %>">
    <div class="card-body">
      <h5 class="card-title"><%= @menu_item.name %></h5>
      <p class="card-text"><%= @menu_item.description %></p>
      <p class="card-text"><strong>Price:</strong> $<%= @menu_item.price %></p>
      <p class="card-text"><strong>Category:</strong> <%= @menu_item.category %></p>
      <p class="card-text"><strong>Day:</strong> <%= @menu_item.day %></p>
      <div class="d-flex justify-content-between">
        <%= link_to "Back to Menu Items", admin_menu_items_path, class: 'btn btn-secondary' %>
        <%= button_to "Delete Menu Item", 
              admin_menu_item_path(@menu_item), 
              method: :delete, 
              data: { confirm: "Are you sure you want to delete this item?" }, 
              class: "btn btn-danger" %>
      </div>
    </div>
  </div>
</div>
